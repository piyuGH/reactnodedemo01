import actionTypes from '../constants/actionTypes';

function provinceSaved() {
    return {
        type: actionTypes.PROVINCE_SAVED
    }
}

export function submitProvince(data) {
    return dispatch => {
        return fetch('/',{
            
        })
    }
}